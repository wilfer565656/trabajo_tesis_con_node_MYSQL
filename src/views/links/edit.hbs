<div class="col-md-4 mx-auto">

    <div class="card">
        <div class="card-body">
            <form action="/links/edit/{{usuario.id}}" method="POST">
            <div class="form-group">
              <input type="type" name="fullname" placeholder="Full Name" value="{{usuario.fullname}}" class="form-control" autofocus>
            </div>
                <div class="form-group">
                <input type="text" name="username" placeholder="Username"  value="{{usuario.username}}"  class="form-control">
                </div>
                <div class="form-group">
                <input type="password" name="password" placeholder="Password" value="{{usuario.password}}" class="form-control">
                </div>
                <div class="form-group">
                <select name="rol" id="" aria-placeholder=" rol" class="form-control"  value="{{usuario.rol}}">
                    <option value="est">estudiante</option>
                    <option value="ase">asesor</option>
                </select>
                </div>
                
                <div class="form-group">
                    <button class="btn btn-success btn-block">
                        Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>